/**
 * Contenedor visual unificado en la landing (mismo lenguaje que el bloque de catálogo).
 */
export const LANDING_ABSTRACT_BG_URL = "/landing-abstract-bg.png";

export const LANDING_PAGE_GUTTER =
  "mx-auto w-full max-w-[1400px] px-3 sm:px-4 lg:px-5";

export const LANDING_SECTION_PANEL =
  "rounded-3xl border border-zinc-800 bg-zinc-900/40 backdrop-blur-sm";

/** Padding vertical como antes; menos margen horizontal dentro del panel. */
export const LANDING_SECTION_PANEL_PAD = "py-6 sm:py-8 px-3.5 sm:px-4";
